# Workspace Rules

## Git Automation Rule
- **Always Git Push**: After completing code changes, edits, or additions in the repository, automatically stage, commit with a descriptive message, and `git push` to the remote repository.
